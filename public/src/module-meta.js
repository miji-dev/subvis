SubVis.ModuleMeta = (function () {
	var that = {},
		$el,

		init = function () {
			$el = $('#module-meta');
			return that;
		},

		render = function () {
			
		};

	that.init = init;
	that.render = render;
	return that;
})();