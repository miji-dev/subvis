#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../nlp_compromise/bin/nlp.js" "$@"
  ret=$?
else 
  node  "$basedir/../nlp_compromise/bin/nlp.js" "$@"
  ret=$?
fi
exit $ret
